# 2026-02-24

## CNG prompt optimization continuity notes
- User clarified live note-generation model is NOT governed by `config/config.json` model path.
- Live llama-server on `:8081` is manually started from local workstation CMD.
- Live model used for note-generation testing: `Ministral 3 14B Q5_K_M GGUF`.
- User preference: keep project information continuously captured, organized, and updated in memory files.
- User runs Open WebUI at `ieissa.com:8443` and SearXNG via docker (container listens on `8080`, observed host mapping includes `8083:8080`).
- Followup optimization loop run completed with stop reason `significant_worsening` and slots idle=true at end.
- User-defined shorthand: call model on `:8081` `ministral14`.
- User clarified `ministral14` is vision-capable (image processing supported).
