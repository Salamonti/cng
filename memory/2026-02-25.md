- Added reusable SearXNG helper scripts in workspace:
  - scripts/searx_search.py
  - scripts/searx_search.sh
- Helpers use SEARXNG_PROXY_URL + SEARXNG_API_KEY env vars and print top results in compact format.
- User preference update: always clarify command execution context (VPS vs workstation) and working path before giving commands.
- Enhanced SearX helper:
  - Added `--json` mode (`scripts/searx_search.sh --json "query"`).
  - Kept compact human-readable default mode with limit support.
- Suggested alias usage for faster invocation from shell.
- User requested durable documentation of SearXNG/OpenClaw setup for future maintenance.
- Added detailed runbook: `memory/projects/searxng-openclaw-setup.md` with topology, security model, env paths, systemd override, helper scripts, and troubleshooting checks.
- Updated `TOOLS.md` with key operational references for SearX endpoint + VPS runtime secrets wiring.
