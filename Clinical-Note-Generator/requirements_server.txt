# C:\Clinical-Note-Generator\requirements_server.txt
# requirements_server.txt
# Unified requirements for Clinical Note Generator server-side
# Covers optimized RTX 5090 workstation and lightweight laptop setups

# --- Core server ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.4.0
requests>=2.31.0
aiohttp>=3.9.0
aiofiles>=23.0.0
httpx>=0.25.0

# --- Performance (skip on Windows if needed) ---
uvloop>=0.17.0; sys_platform != "win32"
httptools>=0.6.0; sys_platform != "win32"

# --- AI / ML ---
# Install CUDA-enabled torch on workstation (adjust version to your CUDA)
# Example: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.35.0
tokenizers>=0.15.0
faster-whisper>=0.9.0
huggingface_hub>=0.20.0

# --- Audio processing ---
sounddevice>=0.4.6
numpy>=1.24.0

# --- OCR / documents ---
pillow>=10.0.0
pymupdf>=1.23.0

# --- Utilities & monitoring ---
psutil>=5.9.0
pynvml>=11.5.0
reportlab>=4.0.0

# --- Development / testing (optional) ---
pytest>=7.4.0
